from google.cloud import storage

def list_bucket_object(bucketname):
    bucketname = "sharadhi-hu19-bucket" 
    storage_client = storage.Client()

    blobs = storage_client.list_blobs(bucketname) 
    
    # List of all objects in bucket
    for blob in blobs:
        print(blob.name)